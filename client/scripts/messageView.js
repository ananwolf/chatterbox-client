var MessageView = {

  render: _.template(

    '<div class="chat">' +
      '<div class="username"></div>' +
      '<div></div>' +
    '</div>'
  )

  // html: MessageView.render

  // append: $chats.append(MessageView.html)
};
// MessagesView.append();
